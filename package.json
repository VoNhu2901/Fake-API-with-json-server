{
  "name": "json-server",
  "version": "1.0.0",
  "description": "demo how to use json-server (Fake API)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "npm run generate-data && nodemon main.js",
    "generate-data": "node generate-data.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "casual": "^1.6.2",
    "fs": "^0.0.1-security",
    "json-server": "^0.17.0",
    "query-string": "^7.1.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
